# AGENTS.md — 海外游戏热词站上站模板

> 用法：把本文件复制到新项目根目录并重命名为 `AGENTS.md`，再把仓库中的 `skills/` 复制到新项目的 `.agents/skills/`。替换 `[项目名]`、`[游戏名]`、`[正式域名]` 等占位符后开始工作。

本文件是总控层，不重复五个 Skill 的全部细节。具体任务必须按本文件的路由加载相应 `SKILL.md`，再按其中的“Required references”读取相关 reference。

## 1. 项目身份和目标

你正在维护的是 `[项目名]` 海外游戏热词站，不是 `[游戏名]` 游戏本体，也不是游戏下载站。

目标链路：

```text
搜索需求
→ 关键词和页面矩阵
→ 可靠来源与真实素材
→ 首页、Wiki 和攻略内页
→ 本地验证
→ 公网部署
→ GSC/GA 数据
→ SEO 复盘、补页或继续观察
```

网站第一任务是让玩家快速搜索、浏览分类和完成操作：

- 首页提供搜索、热门条目、分类入口和新手入口；
- Wiki 承接资源、建筑、机制、平台和更新等查询；
- Guides 承接 Map、Water、Lava、Animals、工厂和新手任务；
- 官方商店和社区链接只服务于真实的平台访问或证据需求；
- 数据产生后再决定补页、改造、继续观察或停止扩张。

### 1.1 项目配置

| 配置 | 值 |
|---|---|
| 项目名 | `[项目名]` |
| 游戏名 | `[游戏名]` |
| 主词 | `[主关键词]` |
| 正式域名 | `[正式域名]` |
| Canonical 协议 | `[https://]` |
| 目标语言 | `[语言]` |
| 技术栈 | `[技术栈]` |
| 资料目录 | `Docs/` |
| 素材目录 | `public/assets/` |

未确认的配置写“未确认”，不要从项目名称、旧站或环境变量猜测。

## 2. 开始任何任务前

先做只读检查：

1. 读取项目根目录 `README.md` 和本 `AGENTS.md`；
2. 读取与当前任务相关的 Docs、作业、研究或 SEO 报告；
3. 检查相关代码、素材、部署脚本和 `git status --short --branch`；
4. 确认当前状态：资料、网站实现、本地验证、GitHub、公网部署、索引和数据分别到哪一步；
5. 保留用户已有修改，不用 `git reset`、`git checkout` 或批量覆盖清理。

只有在确定目标、范围和修改位置后才编辑。

## 3. 生财手册规则

当用户要求查看、核对、搜索或总结生财有术航海手册、关卡、任务卡或作业要求时，必须使用生财 MCP，不直接用浏览器或通用搜索替代。

读取顺序：

```text
activitySearch 或 activityList
→ 确认 activityId
→ activityManualToc
→ activityManualDetail
```

要求：

- 不根据 URL 或名称自行猜 `activityId`；
- `activityManualDetail` 只使用同一次目录返回且 `hasContent=true` 的 `itemId`；
- 返回 `truncated=true` 时继续使用 `nextOffset`；
- MCP 不可用时说明原因，再选择替代读取方式；
- 读取手册是只读操作；提交作业、评论、点赞、收藏或修改站内数据必须得到明确授权。

## 4. 全局来源、事实和素材规则

### 4.1 来源优先级

```text
游戏官网
→ 官方 Steam/平台商店
→ 官方 Wiki、官方新闻和官方 YouTube
→ 可靠媒体
→ Steam 社区、Reddit、社区指南和玩家视频
```

Google 用于发现搜索表达，不把搜索摘要当事实。社区来源只能补充玩家语言、实际案例或待复核线索，不能覆盖官方平台标签或变成官方机制说明。

### 4.2 证据状态

每个页面和关键事实使用一个状态：

| 状态 | 含义 |
|---|---|
| 已交叉验证 | 多个可靠来源一致，或权威官方来源足够 |
| 单一官方来源 | 一个官方页面支持，发布时保留链接和复核日期 |
| 社区线索待复核 | 可作为案例或研究线索，不能写成确定事实 |
| 暂缓发布 | 证据不足或风险过高，保留需求但不扩写 |

动态事实（发售、版本、价格、评价、语言、平台、Game Pass、更新）必须带采集日期。

证据不足时写“暂无可靠公开资料”或“待复核”，不要编造配方、数值、兑换码、联机模式、平台范围、链接或字幕内容。

### 4.3 素材规则

- `public/assets/` 只放明确要发布且可追溯的图片、图标或静态素材；
- 新图片必须同步更新素材清单和 `public/assets/asset-manifest.json`；
- 记录文件名、尺寸、用途、来源、归属、采集日期和使用边界；
- 使用有意义的 `alt`；
- 参考站只用于结构研究，不作为游戏素材来源；
- “官方来源”不等于已经获得商业再分发授权。

## 5. Skill 路由和协作

### 5.1 选择主 Skill

| 用户请求 | 主 Skill | 读取重点 |
|---|---|---|
| 查资料、挖词、做页面矩阵 | `game-site-research` | 研究来源、页面矩阵和关键词素材 references |
| 搭整站、首页、Wiki、Guide | `game-site-level4-builder` | 参考对象、内容模型、路由、素材和本地验证 references |
| 做一个具体 Wiki/Guide 内页 | `wiki-page-builder` | 页面任务、动作链、视觉和验收 references |
| 找视频时间点或实机截图 | `youtube-keyframe-extractor` | 视频访问、字幕定位、抽帧和素材登记 references |
| 看 GSC、和 SEO Agent 沟通、改站 | `seo-agent-site-optimization` | GSC、Agent 对话、站内改造和报告 references |

### 5.2 标准顺序

```text
game-site-research
→ game-site-level4-builder
→ wiki-page-builder
```

页面需要视频证据时：

```text
youtube-keyframe-extractor
→ wiki-page-builder 接入正文
```

上线并产生真实数据后：

```text
seo-agent-site-optimization
→ game-site-research 核验新词
→ wiki-page-builder 生产新页
```

不要因为 SEO Agent 提到一个词，就跳过来源研究直接批量生成页面。

### 5.3 Reference 读取原则

- `SKILL.md` 保留触发条件、硬边界、主流程、输出和完成标准；
- 只有当前任务需要时才读取对应 references；
- 需要图片时读取视觉和素材 reference；
- 需要 GSC 时读取 GSC 和 Agent reference；
- 需要发布报告时读取报告模板；
- 不把项目事实、当前数据或账号状态写进通用 Skill reference。

## 6. 状态不可混写

```text
资料已整理
≠ 网站已实现
≠ 本地可访问
≠ GitHub 已提交
≠ 公网已部署
≠ sitemap 已提交
≠ URL 已请求索引
≠ URL 已被搜索引擎收录
≠ 实时测试访问是长期搜索流量
```

报告和回复必须写明实际证据，不用“已上线”“已收录”“有流量”等模糊词替代验证。

## 7. 文件和编辑规则

- 所有项目文件放在项目目录内；
- 文档、关键词、研究资料和报告放在 `Docs/`；
- 使用 `apply_patch` 编辑；
- 不删除、重命名或覆盖用户已有文件，除非明确要求；
- JSON 使用 `jq empty` 或等价工具检查；
- Markdown 能被普通编辑器直接阅读；
- 不写入 API Key、密码、Cookie、Cloudflare Token、GSC/GA 密钥或其他秘密；
- 不复制旧项目的域名、游戏名、sitemap、robots、统计 ID、品牌、文案或外链。

## 8. 外部操作权限

未经用户明确授权，不执行：

- 公网部署、DNS、Cloudflare、GSC、GA 或 Bing 账户操作；
- sitemap 提交、URL 索引请求；
- Reddit、Steam、Discord、论坛、评论或社交媒体发帖；
- 第三方 Wiki、目录、数据库编辑；
- 联系开发者、作者、站长或媒体；
- 创建账号、购买链接、广告、赞助或其他付费动作。

可以先做只读研究、本地修改、草稿和验证。授权最终外部操作前，说明目的地、内容、URL、账号上下文和将产生的状态变化。

## 9. 通用质量门槛

交付前检查：

- [ ] 关键词对应明确的搜索意图；
- [ ] 页面和关键事实有来源及证据状态；
- [ ] 动态事实有采集日期；
- [ ] 没有坏链接、可疑下载、假来源或竞品内容；
- [ ] 首页、Wiki、Guide 和条目可互相点击；
- [ ] 单页能指导玩家完成最小成功动作；
- [ ] 视频关键帧不是封面，且有时间点和归因；
- [ ] 新图片已登记素材清单和 manifest；
- [ ] Title、Description、单一 H1、H2/H3、canonical 已检查；
- [ ] JSON、typecheck、build、diff 和本地路由检查已完成；
- [ ] 本地实现、部署、索引、收录和流量没有混写；
- [ ] 外部动作均有明确授权。

## 10. 每次任务的最终交付

最终回复必须说明：

1. 修改了哪些文件；
2. 完成了哪个 Skill 阶段或项目关卡；
3. 哪些内容未完成及原因；
4. 做了哪些验证及结果；
5. 是否执行公网、账户或公开发布操作；
6. 下一步最小行动是什么。

没有执行外部动作时，明确写：

```text
本轮未执行发帖、外链、联系、账户创建、索引请求、sitemap 提交或公网部署。
```
